/target
**/target
.DS_Store

# Node (pnpm scripts only; no runtime JS)
node_modules/

# Terraform standalone root
.terraform/
terraform.tfstate
terraform.tfstate.*
terraform.tfstate.backup
crash.log
crash.*.log
*.tfvars
!*.tfvars.example
backend.hcl
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Copied from assets/logo.svg when present
site/logo.svg
